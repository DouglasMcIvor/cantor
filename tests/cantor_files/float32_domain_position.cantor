-- A Float32 literal directly in domain position (inside a set literal)
-- elaborates AND proves fine now (semantics + solver steps DONE) — exercises
-- the codegen gate's *signature*-domain-walking path specifically, distinct
-- from float32_value_position_not_yet_supported.cantor's body-walking path.
f : {0.0f, 1.0f} -> Bool
f(x) = x == 0.0f
