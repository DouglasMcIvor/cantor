-- Float32 literals lex and parse today, but semantics/solver/codegen
-- support isn't implemented yet (see docs/design-decisions.md's
-- `Float32`/`FiniteFloat32` section) — this must fail loudly as
-- "not yet supported", never silently misclassified as `Int` or crash.
main : -> Int
main() = 3.14f
