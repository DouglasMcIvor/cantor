-- `"Hello"` desugars to a Tuple of CharLits, which coerces to `Vector(Char)`
-- via the same machinery `[char(72), char(101), ...]` already exercises —
-- printed as text, not a list of codepoints.
main : -> Char*
main() = "Hello"
